CREATE TABLE user(
userID VARCHAR(20),
userPassword VARCHAR(20),
userName VARCHAR(20),
userEmail VARCHAR(50),
userTel VARCHAR(20),
userSex VARCHAR(20),
userAge VARCHAR(20),
PRIMARY KEY(userID)
)default CHARSET=utf8;